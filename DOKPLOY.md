# Развертывание SubSpark в Dokploy

Этот гайд поможет развернуть SubSpark в Dokploy используя Dockerfile.

## Предварительные требования

- ✅ Dokploy установлен и работает
- ✅ MySQL база данных создана
- ✅ Redis установлен и доступен
- 📦 Git репозиторий (GitHub, GitLab, Gitea или любой другой)

## Шаг 1: Подготовка базы данных

Если вы еще не создали базу данных в Dokploy:

```sql
CREATE DATABASE subspark CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'subspark'@'%' IDENTIFIED BY 'ваш_надежный_пароль';
GRANT ALL PRIVILEGES ON subspark.* TO 'subspark'@'%';
FLUSH PRIVILEGES;
```

Импортируйте схему базы данных (если есть файл schema.sql или аналогичный).

## Шаг 2: Создание приложения в Dokploy

### 2.1 Создайте новый проект
1. Откройте панель Dokploy
2. Нажмите "Create Project" → выберите "Application"
3. Выберите тип: **Docker**

### 2.2 Настройка Git репозитория
1. Подключите ваш Git репозиторий
2. Выберите ветку: `dokploy`
3. Build Context: `.` (корень проекта)
4. Dockerfile Path: `./Dockerfile`

### 2.3 Настройка портов
- Container Port: `80`
- Dokploy автоматически назначит внешний порт или можете указать свой

## Шаг 3: Переменные окружения

В разделе "Environment Variables" добавьте следующие переменные:

### Обязательные переменные базы данных
```bash
DB_HOST=ваш_mysql_хост          # Например: mysql (если в том же Dokploy)
DB_NAME=subspark
DB_USER=subspark
DB_PASSWORD=ваш_надежный_пароль
DB_PORT=3306
```

### Обязательные переменные Redis
```bash
REDIS_HOST=ваш_redis_хост       # Например: redis (если в том же Dokploy)
REDIS_PORT=6379
REDIS_PASSWORD=                  # Оставьте пустым если без пароля
REDIS_DB=0
```

### Обязательные переменные приложения
```bash
APP_ENV=production
APP_URL=https://ваш-домен.com
BASE_URL=https://ваш-домен.com
```

### FFmpeg (уже настроен в контейнере)
```bash
FFMPEG_STATUS=1
FFMPEG_PATH=/usr/bin/ffmpeg
FFMPEG_PROBE=/usr/bin/ffprobe
```

### Асинхронная обработка видео (рекомендуется)
```bash
USE_ASYNC_VIDEO_PROCESSING=1    # 1 = асинхронно, 0 = синхронно
```

### Опциональные: Хранилища (выберите одно или несколько)

#### Selectel S3 (рекомендуется для РФ)
```bash
SELECTEL_STATUS=1
SELECTEL_BUCKET=ваш-bucket
SELECTEL_REGION=ru-1
SELECTEL_KEY=ваш-access-key
SELECTEL_SECRET=ваш-secret-key
SELECTEL_ENDPOINT=https://s3.ru-1.storage.selcloud.ru
SELECTEL_PUBLIC_BASE=https://ваш-bucket.s3.ru-1.storage.selcloud.ru
```

#### MinIO
```bash
MINIO_STATUS=1
MINIO_BUCKET=subspark
MINIO_REGION=us-east-1
MINIO_KEY=ваш-access-key
MINIO_SECRET=ваш-secret-key
MINIO_ENDPOINT=https://minio.ваш-домен.com
MINIO_PUBLIC_BASE=https://minio.ваш-домен.com/subspark
MINIO_PATH_STYLE=1
MINIO_SSL_VERIFY=0
```

#### AWS S3
```bash
S3_STATUS=1
S3_BUCKET=ваш-bucket
S3_REGION=us-east-1
S3_KEY=ваш-access-key
S3_SECRET=ваш-secret-key
S3_PUBLIC_BASE=https://ваш-bucket.s3.amazonaws.com
```

## Шаг 4: Настройка Volumes (важно!)

Для сохранения загруженных файлов нужно создать постоянное хранилище:

1. В Dokploy перейдите в раздел "Volumes"
2. Создайте volume с названием `subspark-uploads`
3. Mount path: `/var/www/html/uploads`

**Примечание:** Если используете S3/Selectel/MinIO, volume всё равно нужен для временных файлов при обработке.

## Шаг 5: Настройка домена и SSL

1. В разделе "Domains" добавьте ваш домен
2. Dokploy автоматически настроит SSL через Let's Encrypt
3. Подождите 1-2 минуты пока выпустится сертификат

## Шаг 6: Деплой!

1. Нажмите кнопку **"Deploy"**
2. Следите за логами сборки
3. Сборка займет 3-5 минут (скачивание зависимостей, установка PHP extensions)

### Проверка логов
После деплоя проверьте логи:
```bash
# В Dokploy UI откройте вкладку "Logs"
# Должны увидеть:
# [supervisor] nginx started
# [supervisor] php-fpm started
```

## Шаг 7: Проверка работоспособности

Откройте ваш домен в браузере:
```
https://ваш-домен.com
```

Должна открыться главная страница SubSpark.

## Шаг 8 (Опционально): Развертывание Video Worker

Если включили `USE_ASYNC_VIDEO_PROCESSING=1`, нужно развернуть worker:

### 8.1 Создайте еще одно приложение в Dokploy
- Название: `subspark-worker`
- Тип: Docker
- Dockerfile Path: `./worker/Dockerfile`

### 8.2 Переменные окружения для worker
```bash
REDIS_HOST=ваш_redis_хост
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
FFMPEG_PATH=/usr/bin/ffmpeg
FFMPEG_PROBE=/usr/bin/ffprobe
```

### 8.3 Volume для worker
- Volume: тот же `subspark-uploads`
- Mount path: `/app/uploads`

### 8.4 Ресурсы (рекомендуется ограничить)
В Dokploy можно ограничить ресурсы:
- CPU Limit: 2 cores
- Memory Limit: 2GB

## Troubleshooting

### Приложение не запускается
```bash
# Проверьте логи в Dokploy UI
# Типичные проблемы:
# 1. Неверные credentials БД
# 2. Redis недоступен
# 3. Нехватка памяти
```

### Ошибки подключения к БД
```bash
# Проверьте что MySQL доступен из контейнера:
# В Dokploy откройте терминал контейнера:
mysql -h $DB_HOST -u $DB_USER -p$DB_PASSWORD $DB_NAME
```

### Ошибки Redis
```bash
# Проверьте подключение:
redis-cli -h $REDIS_HOST -p $REDIS_PORT ping
# Должен вернуть: PONG
```

### Проблемы с загрузкой файлов
1. Проверьте что volume подключен правильно
2. Проверьте права доступа к директории uploads
3. Если используете S3/Selectel - проверьте credentials

### FFmpeg не работает
```bash
# Проверьте что FFmpeg установлен в контейнере:
ffmpeg -version
ffprobe -version
```

## Обновление приложения

Для обновления:
1. Сделайте `git push` в ветку `dokploy`
2. В Dokploy нажмите "Redeploy"
3. Dokploy пересоберет образ и перезапустит контейнер

## Мониторинг и логи

### Логи приложения
- Dokploy UI → вкладка "Logs"
- Логи Nginx: `/var/log/nginx/error.log`
- Логи PHP-FPM: stdout/stderr

### Метрики
Dokploy показывает:
- CPU usage
- Memory usage
- Network traffic
- Container status

## Оптимизация производительности

### 1. Redis cache прогрев
После первого деплоя cache будет пустой. Первые запросы будут медленнее.

### 2. OPcache
OPcache уже настроен и включен в Dockerfile. JIT компиляция активна.

### 3. Nginx buffers
Уже оптимизированы для загрузки больших файлов (до 128MB).

### 4. PHP-FPM
Настроен на обработку длительных запросов (до 10 минут для FFmpeg).

## Резервное копирование

### База данных
```bash
# Настройте автоматические бэкапы в Dokploy или используйте cron:
mysqldump -h $DB_HOST -u $DB_USER -p$DB_PASSWORD $DB_NAME > backup.sql
```

### Файлы (если не используете S3)
Dokploy volumes можно бэкапить через его UI или вручную:
```bash
docker volume inspect subspark-uploads
# Скопируйте данные из Mountpoint
```

### Переменные окружения
Экспортируйте переменные окружения из Dokploy UI (есть кнопка Export).

## Безопасность

1. ✅ Используйте сильные пароли для БД
2. ✅ Включите firewall на сервере Dokploy
3. ✅ Регулярно обновляйте зависимости
4. ✅ Мониторьте логи на подозрительную активность
5. ✅ Используйте HTTPS (автоматически через Let's Encrypt)
6. ✅ Ограничьте доступ к БД только для приложения

## Полезные команды

### Просмотр логов в реальном времени
```bash
# В Dokploy UI → Logs → включите "Follow"
```

### Подключение к контейнеру
```bash
# В Dokploy UI → Terminal
# Или через CLI:
docker exec -it subspark-app sh
```

### Очистка кеша Redis
```bash
redis-cli -h $REDIS_HOST -p $REDIS_PORT FLUSHALL
```

### Проверка статуса FFmpeg
```bash
# Через терминал контейнера:
ffmpeg -version
ps aux | grep ffmpeg  # Посмотреть запущенные процессы
```

## Контакты и поддержка

- GitHub Issues: https://github.com/ваш-репозиторий/issues
- Dokploy Docs: https://docs.dokploy.com

---

**Готово!** Ваше приложение SubSpark теперь работает в Dokploy! 🚀
