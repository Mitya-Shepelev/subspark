# Инструкция по деплою SubSpark в Portainer

## 📋 Обзор

После каждого `git push` в ветку `main` автоматически:
1. GitHub Actions собирает 2 Docker образа
2. Образы публикуются в GitHub Container Registry
3. Нужно обновить stack в Portainer для применения изменений

## 🏗️ Автоматическая сборка образов

### Собираются 2 образа:

**1. Основное приложение:**
```
ghcr.io/mitya-shepelev/subspark:latest
```
- Nginx + PHP-FPM + Supervisor
- FFmpeg встроен в контейнер
- Все приложение

**2. Video Worker (для будущего):**
```
ghcr.io/mitya-shepelev/subspark-worker:latest
```
- PHP CLI + FFmpeg
- Обрабатывает видео асинхронно через Redis очередь
- НЕ используется по умолчанию (требует активации)

### Проверка статуса сборки:

https://github.com/Mitya-Shepelev/subspark/actions

Время сборки: обычно 3-5 минут

## 🚀 Процедура деплоя в Portainer

### Шаг 1: Дождаться завершения GitHub Actions

1. Откройте https://github.com/Mitya-Shepelev/subspark/actions
2. Найдите последний workflow "Build and Push Docker Image"
3. Дождитесь зеленой галочки ✅
4. Убедитесь что оба job'а завершились:
   - ✅ Build Main Application Image
   - ✅ Build Video Worker Image

### Шаг 2: Войти в Portainer

1. Откройте Portainer (ваш URL)
2. Перейдите в **Stacks**
3. Найдите stack **subspark**

### Шаг 3: Обновить stack

1. Нажмите на имя stack **subspark**
2. Внизу страницы нажмите кнопку **"Editor"**
3. Проверьте что конфигурация актуальна (должна включать FFmpeg переменные)
4. Нажмите **"Update the stack"**
5. **ВАЖНО:** Включите галочку **"Re-pull image and redeploy"**
6. Нажмите **"Update"**

### Шаг 4: Проверить обновление

1. Дождитесь перезапуска контейнеров (обычно 30-60 секунд)
2. Откройте сайт и проверьте что все работает
3. Проверьте версию в логах:
   ```bash
   docker logs subspark-app --tail 50
   ```

## ⚙️ Переменные окружения в Portainer

### Обязательные переменные (уже должны быть настроены):

```env
# Database
DB_SERVER=mysql-8-mysql_db-1
DB_USERNAME=subspark
DB_PASSWORD=ваш_пароль
DB_DATABASE=subspark

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Selectel Storage
SELECTEL_STATUS=1
SELECTEL_BUCKET=ваш_bucket
SELECTEL_REGION=ru-1
SELECTEL_KEY=ваш_ключ
SELECTEL_SECRET=ваш_секрет
SELECTEL_ENDPOINT=https://s3.ru-1.storage.selcloud.ru
SELECTEL_PUBLIC_BASE=https://ваш_bucket.s3.ru-1.storage.selcloud.ru
```

### Новые переменные FFmpeg (добавлены автоматически):

```env
# FFmpeg Configuration
FFMPEG_STATUS=1
FFMPEG_PATH=/usr/bin/ffmpeg
FFMPEG_PROBE=/usr/bin/ffprobe

# Async Video Processing (пока отключено)
USE_ASYNC_VIDEO_PROCESSING=0
```

**Примечание:** Эти переменные уже прописаны в `portainer-stack.yml` с значениями по умолчанию. Переопределять их в Environment Variables нужно только если хотите изменить значения.

## 🎯 Текущий режим работы

### FFmpeg работает в основном контейнере (синхронно):

```
User Upload → Web Server → FFmpeg (обработка) → Response
              ↓ (пользователь ждет 10-30 сек)
```

**Плюсы:**
- ✅ Простая конфигурация
- ✅ Нет дополнительных контейнеров

**Минусы:**
- ❌ Пользователь ждет обработку
- ❌ Нагружает CPU веб-сервера
- ❌ При множестве загрузок сайт может тормозить

## 🔮 Активация асинхронной обработки (опционально)

### Когда активировать:

- Когда появится много пользователей (50+ онлайн)
- Когда сервер начнет тормозить при загрузке видео
- Когда нужна высокая производительность

### Как активировать:

**1. Применить изменения БД:**

См. полную инструкцию в файле `АРХИТЕКТУРА_ВИДЕО_ОБРАБОТКИ.md`

Нужно добавить поля `processing_status` и `processing_job_id` в таблицы.

**2. Обновить `portainer-stack.yml`:**

Раскомментировать секцию `video-worker` (строки 82-116):

```yaml
  video-worker:
    image: ghcr.io/mitya-shepelev/subspark-worker:latest
    container_name: subspark-video-worker
    restart: unless-stopped
    pull_policy: always
    networks:
      - redis_default
    volumes:
      - uploads_data:/app/uploads:rw
    environment:
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-0}
      - FFMPEG_PATH=/usr/bin/ffmpeg
      - FFMPEG_PROBE=/usr/bin/ffprobe
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
```

**3. Включить асинхронную обработку:**

В Environment Variables или в `portainer-stack.yml`:

```env
USE_ASYNC_VIDEO_PROCESSING=1
```

**4. Update stack в Portainer**

## 🔍 Проверка работы FFmpeg

### После деплоя проверьте:

**1. Войдите в админ панель:**
```
https://ваш-сайт.com/admin
```

**2. Перейдите в Настройки → FFmpeg**

**3. Проверьте настройки:**
- FFmpeg Path: `/usr/bin/ffmpeg`
- FFprobe Path: `/usr/bin/ffprobe`
- FFmpeg Status: `Включен`

**4. Загрузите тестовое видео:**
- Создайте reel или обычный пост с видео
- Проверьте что видео конвертируется и миниатюра создается

### Проверка через консоль:

```bash
# Войти в контейнер
docker exec -it subspark-app sh

# Проверить FFmpeg
ffmpeg -version

# Проверить FFprobe
ffprobe -version

# Проверить права на uploads
ls -la /var/www/html/uploads

# Выйти
exit
```

## 📊 Мониторинг

### Логи контейнера:

```bash
# Последние 100 строк
docker logs subspark-app --tail 100

# Следить в реальном времени
docker logs -f subspark-app

# Только ошибки
docker logs subspark-app 2>&1 | grep ERROR
```

### Использование ресурсов:

```bash
# Статистика контейнера
docker stats subspark-app

# Использование диска
docker exec subspark-app du -sh /var/www/html/uploads
```

### Проверка Redis (для будущего worker):

```bash
# Войти в Redis
docker exec -it redis redis-cli

# Проверить очередь видео
LLEN video_processing_queue

# Проверить активные задачи
SMEMBERS video_processing_active

# Выйти
exit
```

## ⚠️ Troubleshooting

### Проблема: Образ не обновился

**Решение:**
1. Проверьте что GitHub Actions завершился успешно
2. В Portainer обязательно включите **"Re-pull image and redeploy"**
3. Проверьте hash образа:
   ```bash
   docker inspect subspark-app | grep Image
   ```

### Проблема: FFmpeg не работает

**Решение:**
1. Проверьте переменные окружения в контейнере:
   ```bash
   docker exec subspark-app env | grep FFMPEG
   ```
2. Проверьте что FFmpeg установлен:
   ```bash
   docker exec subspark-app ffmpeg -version
   ```
3. Проверьте логи при загрузке видео

### Проблема: Видео не загружается

**Возможные причины:**
1. Нет прав на запись в `/var/www/html/uploads`
2. Недостаточно места на диске
3. FFmpeg не может обработать формат видео
4. Превышен лимит памяти PHP

**Решение:**
```bash
# Проверить права
docker exec subspark-app ls -la /var/www/html/uploads

# Проверить место
docker exec subspark-app df -h

# Проверить логи
docker logs subspark-app --tail 200 | grep -i error
```

### Проблема: Сайт тормозит при загрузке видео

**Временное решение:**
Увеличить лимиты PHP в Dockerfile:
```ini
memory_limit=512M
max_execution_time=600
```

**Долгосрочное решение:**
Активировать асинхронную обработку через worker (см. выше)

## 🚨 Откат к предыдущей версии

Если что-то пошло не так:

### Вариант 1: Откатить stack в Portainer

1. В Portainer откройте stack
2. Нажмите **"Editor"**
3. Скопируйте старую конфигурацию из Git истории
4. Нажмите **"Update the stack"**

### Вариант 2: Использовать старый образ

```bash
# Остановить контейнер
docker stop subspark-app

# Удалить контейнер
docker rm subspark-app

# Запустить со старым тегом
docker run -d --name subspark-app \
  ghcr.io/mitya-shepelev/subspark:main \
  ... (остальные параметры)
```

### Вариант 3: Откатить коммит в Git

```bash
git revert HEAD
git push
```

Дождитесь сборки и обновите stack в Portainer.

## 📞 Поддержка

### Полезные файлы документации:

- `README.md` - Общая информация о проекте
- `CLAUDE.md` - Инструкции для разработки
- `АРХИТЕКТУРА_ВИДЕО_ОБРАБОТКИ.md` - Детали асинхронной обработки видео
- `worker/README.md` - Документация по video worker

### Логи и диагностика:

```bash
# Все логи приложения
docker logs subspark-app > logs.txt

# Логи FFmpeg (если есть)
docker exec subspark-app cat /var/log/ffmpeg.log

# Логи Nginx
docker exec subspark-app cat /var/log/nginx/error.log
```

## ✅ Checklist после каждого деплоя

- [ ] GitHub Actions завершился успешно (зеленая галочка)
- [ ] Stack обновлен в Portainer с "Re-pull image"
- [ ] Контейнер перезапустился без ошибок
- [ ] Сайт открывается и работает
- [ ] Можно войти в админ панель
- [ ] FFmpeg настроен правильно (проверить в настройках)
- [ ] Загрузка видео работает (создать тестовый пост)
- [ ] Selectel storage работает (файлы загружаются в облако)
- [ ] Проверены логи на наличие ошибок

---

**Дата последнего обновления:** 2025-11-11
**Версия документации:** 1.0
